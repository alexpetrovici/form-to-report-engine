<section>
  <h2>{{title}}</h2>

  {{#each signers}}
    <div>
      <strong>{{label}}</strong>
      <span>{{name}}</span>

      {{#if signed}}
        <span>Signed</span>
      {{else}}
        <span>Not signed</span>
      {{/if}}
    </div>
  {{/each}}
</section>