<section>
  <h2>{{title}}</h2>

  {{#each signers}}
    <div>
      <strong>{{label}}</strong>
      <span>{{displayName}}</span>

      {{#if signed}}
        <span>Signed</span>
      {{else}}
        <span>Not signed</span>
      {{/if}}
    </div>
  {{/each}}
</section>